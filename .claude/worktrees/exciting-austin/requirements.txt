# ── Dépendances de production ─────────────────────────────────────────────
# Ne lister ici que les bibliothèques réellement importées dans le code.
# Les outils de développement (Faker, debug toolbar…) vont dans requirements-dev.txt.

# Framework principal
Django==5.0.6
asgiref==3.9.1

# Interface d'administration — filtres avancés par plage de dates
django-admin-rangefilter==0.13.3

# Génération PDF (documents officiels, rapports)
weasyprint==68.1
brotli==1.2.0
cffi==2.0.0
cssselect2==0.9.0
fonttools==4.61.1
pycparser==3.0
pydyf==0.12.1
pyphen==0.17.2
tinycss2==1.5.1
tinyhtml5==2.0.0
webencodings==0.5.1
zopfli==0.4.1

# Graphiques / images dans les rapports
reportlab==4.4.3
pillow==11.3.0

# Base de données (production : PostgreSQL)
psycopg2-binary==2.9.11
dj-database-url==2.3.0

# Variables d'environnement
python-decouple==3.8

# Parsing SQL (dépendance interne de Django Debug Toolbar et migrations)
sqlparse==0.5.3

# Serveur WSGI production
gunicorn==25.1.0

# Fichiers statiques en production (sans Nginx)
whitenoise==6.9.0

# Fuseaux horaires
tzdata==2025.2

# Divers
charset-normalizer==3.4.3
packaging==26.0
